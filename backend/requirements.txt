# ── Runtime dependencies (deployed with Lambda) ──────────────────────────────
boto3>=1.34.0
botocore>=1.34.0

# ── Test dependencies (local only — NOT included in Lambda deployment zip) ───
# Install with: pip install -r requirements.txt
moto[all]>=5.0.0   # Fake AWS services for unit testing (no real AWS account needed)
pytest>=8.0.0      # Test runner
